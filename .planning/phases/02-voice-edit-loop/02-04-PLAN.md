---
phase: 02-voice-edit-loop
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - hooks/useEstimateVoice.ts
autonomous: true
requirements:
  - VOICE-03
  - VUX-02
must_haves:
  truths:
    - '"수정" 발화 시 수정 모드에 진입한다'
    - '"그만" 또는 "종료" 발화 시 수정 모드가 종료된다'
    - 'STT 텍스트에 마침표/공백 등 부가 문자가 있어도 키워드가 인식된다'
  artifacts:
    - path: "hooks/useEstimateVoice.ts"
      provides: "onSttText 내 키워드 모드 전환 로직"
---

<objective>
STT 텍스트에서 "수정"/"그만"/"종료" 키워드를 감지하여 음성 편집 모드를 전환한다.

현재 상태:
- "수정" 감지는 Web Speech API(useWakeWord) 경로에만 있음. Web Speech API가 불안정하여 미동작.
- onSttText에 "수정" 감지 코드가 추가되었으나, enterEditModeRef를 통한 호출이 실제로 isEditMode를 true로 바꾸는지 검증 안 됨.
- "그만" 감지는 onSttText에 있으나, editModeRef.current.isEditMode === true일 때만 동작.

해결 방향:
- onSttText에서 STT 텍스트를 정규화(trim, 마침표/물음표 제거) 후 키워드 매칭.
- "수정" → enterEditModeRef.current() 호출. 호출 후 isEditMode가 true가 되는지 확인.
- "그만"/"종료" → exitEditMode 호출. 수정 모드가 아닐 때는 무시.
- Web Speech API 경로는 보조 수단으로 유지하되, STT 경로가 주 진입점.

수정 범위: hooks/useEstimateVoice.ts의 onSttText 콜백만 수정.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: onSttText 키워드 매칭 정규화 + 수정 모드 전환</name>
  <files>hooks/useEstimateVoice.ts</files>
  <action>
1. onSttText 콜백 상단에서 STT 텍스트 정규화:
   ```typescript
   const normalized = text.trim().replace(/[.?!。？！]/g, '')
   ```
2. "수정" 키워드 매칭 (시트 존재 + 수정 모드 아닐 때):
   - 현재: `/수정/.test(text.trim())` + `enterEditModeRef.current()` 호출
   - 확인: enterEditModeRef.current가 useVoiceEditMode.enterEditMode을 가리키는지 확인
   - enterEditMode 내부: `setIsEditMode(true)` + TTS "수정 모드입니다. 말씀하세요."
   - 호출 후 editModeRef.current.isEditMode가 다음 렌더에서 true가 되는지 확인
3. "그만"/"종료" 키워드 매칭:
   - 현재: `/그만|종료|멈춰/.test(text.trim())` + `editModeRef.current.isEditMode` 조건
   - 정규화된 텍스트 사용으로 변경
   - "그만해", "그만.", "종료해줘" 등도 매칭되는지 확인
4. console.log로 키워드 매칭 결과 로그:
   ```typescript
   console.log('[KeywordMatch]', { normalized, isEditMode: editModeRef.current.isEditMode })
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - "수정"이 포함된 STT 텍스트 → enterEditMode 호출 → isEditMode true
  - "그만"/"종료"가 포함된 STT 텍스트 + 수정 모드 → exitEditMode 호출 → isEditMode false
  - 정규화로 "수정.", "그만해" 등 부가 문자 대응
  - 빌드 통과
  </done>
</task>

</tasks>

<verification>
1. npm run build 통과
2. 브라우저 콘솔에서 [STT]와 [KeywordMatch] 로그 확인
3. 실제 테스트: Space 녹음 → "수정" 발화 → 수정 모드 진입 TTS 확인
4. 실제 테스트: 수정 모드에서 "그만" 발화 → 모드 종료 확인
</verification>

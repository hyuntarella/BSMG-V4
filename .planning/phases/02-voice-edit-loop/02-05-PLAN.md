---
phase: 02-voice-edit-loop
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - hooks/useVoice.ts
  - hooks/useVoiceEditMode.ts
autonomous: true
requirements:
  - VOICE-04
  - VUX-03
must_haves:
  truths:
    - 'TTS 재생 완료 후 2초 뒤 자동 녹음이 시작된다'
    - 'TTS 재생 중에는 녹음이 시작되지 않는다'
    - 'TTS가 겹쳐서 재생되지 않는다'
  artifacts:
    - path: "hooks/useVoice.ts"
      provides: "startRecording status 가드, playTts 이전 오디오 중지"
    - path: "hooks/useVoiceEditMode.ts"
      provides: "speaking→idle 전환 감지 → 2초 후 onAutoResume"
---

<objective>
TTS 재생 완료 후 2초 대기하고 자동으로 녹음을 재개한다. TTS 재생 중 녹음 시작을 차단한다.

현재 상태:
- useVoice.playTts: 이전 오디오 중지(audioRef.pause) 있음. onended에서 setStatus('idle') 호출.
- useVoice.startRecording: statusRef.current !== 'idle' 가드 추가됨.
- useVoiceEditMode: speaking→idle 전환 감지 → AUTO_RESUME_DELAY(2초) 후 onAutoResume(startRecording) 호출.

잠재 문제:
- playTts가 async이지만 audio.play()의 Promise는 "재생 시작"에 resolve. onended 전에 return.
- startRecording의 statusRef 가드가 정확히 동작하는지 (setStatus와 statusRef 동기화 확인).
- useVoiceEditMode의 prevStatusRef가 speaking→idle 전환을 정확히 감지하는지.
- 자동 재개 후 또 speaking이 되면 무한 루프 가능성 (재개→녹음→STT→LLM→TTS→재개→...).
  이건 의도된 동작(연속 편집 루프)이지만, 종료 조건("그만", 5초 무음)이 있어야 함.

해결 방향:
- 기존 코드의 동작을 검증하고, 미동작 시 원인 파악 후 최소 수정.
- playTts의 onended → setStatus('idle') → useVoiceEditMode useEffect 트리거 확인.
- 자동 재개 시 startRecording이 실제로 호출되는지 console.log 추가.

수정 범위: hooks/useVoice.ts, hooks/useVoiceEditMode.ts만 수정.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: TTS→자동재개 파이프라인 검증 및 수정</name>
  <files>hooks/useVoice.ts, hooks/useVoiceEditMode.ts</files>
  <action>
1. useVoice.ts의 playTts 확인:
   - onended 콜백에서 setStatus('idle') 호출 확인
   - setStatus가 statusRef.current도 같이 업데이트하는지 확인 (그룹 B에서 래핑 완료)
   - audio.onerror도 처리하여 에러 시 status가 'idle'로 돌아오는지 확인

2. useVoiceEditMode.ts의 자동 재개 useEffect 확인:
   - voiceStatus prop이 useVoice의 status와 동기화되는지 확인
   - prevStatusRef가 'speaking'을 정확히 기록하는지 확인
   - isEditModeRef.current가 true인지 확인 (수정 모드에서만 자동 재개)
   - console.log('[AutoResume]', { prev: prevStatusRef.current, current: voiceStatus, isEditMode }) 추가

3. startRecording의 status 가드 확인:
   - statusRef.current !== 'idle'이면 return
   - 가드가 동작하면 console.log('[StartRecording] blocked — status:', statusRef.current)

4. TTS 겹침 방지 확인:
   - playTts에서 audioRef.current.pause() 호출 확인 (그룹 B에서 추가 완료)
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - TTS 재생 완료 → status 'idle' → useVoiceEditMode 감지 → 2초 후 startRecording 호출
  - TTS 재생 중 startRecording 호출 시 무시됨
  - 브라우저 콘솔에 [AutoResume] 로그로 동작 확인 가능
  - 빌드 통과
  </done>
</task>

</tasks>

<verification>
1. npm run build 통과
2. 수정 모드 진입 → 명령 발화 → TTS 재생 → 2초 후 자동 녹음 시작 확인
3. TTS 재생 중 Space 키로 녹음 시도 → 무시 확인
4. 연속 2회 명령 → TTS가 겹치지 않고 순차 재생 확인
</verification>

---
phase: 02-voice-edit-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - hooks/useVoice.ts
  - hooks/useVoiceEditMode.ts
  - components/estimate/EstimateEditor.tsx
autonomous: true
requirements:
  - VOICE-07
  - VOICE-08
  - VUX-04
  - VUX-05
must_haves:
  truths:
    - '확신도 95%+ 명령은 즉시 실행되고 결과만 TTS로 알린다'
    - '확신도 70-95% 명령은 실행 후 "맞습니까?" TTS가 재생되고, "아니" 응답 시 undo + TTS "되돌렸습니다"'
    - '확신도 70% 미만 명령은 실행되지 않고 되묻기 TTS가 재생된다'
    - '"취소"/"되돌려" 음성 명령으로 직전 스냅샷이 복원된다'
    - '"현재 상태 알려줘"가 TTS로 면적/공종수/총액/마진율을 읽어준다'
    - '"마진 얼마야?"가 TTS로 현재 마진율을 읽어준다'
    - '"면적 200으로 변경" 같은 update_meta 명령이 처리된다'
    - 'VAD 5초 무음 감지로 수정 모드가 자동 종료된다'
  artifacts:
    - path: "hooks/useVoice.ts"
      provides: "pendingConfirm 상태 관리 + clearLastCommand 메서드"
    - path: "hooks/useVoiceEditMode.ts"
      provides: "VAD 통합 (AnalyserNode 기반 5초 무음 감지)"
    - path: "components/estimate/EstimateEditor.tsx"
      provides: "medium confidence 확인 처리 + read_summary/read_margin/update_meta 시스템 명령"
  key_links:
    - from: "hooks/useVoice.ts pendingConfirm"
      to: "components/estimate/EstimateEditor.tsx onSttText"
      via: "pendingConfirmRef check before LLM"
      pattern: "pendingConfirm"
    - from: "hooks/useVoiceEditMode.ts VAD onSilence"
      to: "hooks/useVoiceEditMode.ts exitEditMode"
      via: "AnalyserNode silence detection"
      pattern: "SILENCE_DURATION"
    - from: "EstimateEditor handleVoiceCommands"
      to: "useEstimate updateMeta"
      via: "update_meta action routing"
      pattern: "update_meta"
---

<objective>
확신도 분기 UX를 완성하고, 시스템 명령(요약/마진/update_meta)을 추가하며, VAD 무음 감지를 구현하여 음성 편집 루프의 모든 요구사항을 충족한다.

Purpose: Plan 02-01에서 수정 모드 진입/종료/자동재개 루프가 완성되었지만, medium confidence 확인 응답 처리, 시스템 명령 일부, VAD가 미구현이다. 이 gap을 채워 Phase 2 전체 요구사항을 완성한다.
Output: 확신도 3단계 분기 완전 동작 + read_summary/read_margin/update_meta + VAD 5초 무음 종료
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-voice-edit-loop/02-CONTEXT.md
@.planning/phases/02-voice-edit-loop/02-RESEARCH.md
@.planning/phases/02-voice-edit-loop/02-01-SUMMARY.md

@hooks/useVoice.ts
@hooks/useEstimate.ts
@hooks/useVoiceEditMode.ts
@components/estimate/EstimateEditor.tsx
@lib/voice/commands.ts
@lib/voice/confidenceRouter.ts

<interfaces>
<!-- From 02-01 plan outputs -->

From hooks/useVoiceEditMode.ts (created in 02-01):
```typescript
export function useVoiceEditMode(options: UseVoiceEditModeOptions): {
  isEditMode: boolean
  enterEditMode: () => Promise<void>
  exitEditMode: () => void
}
```

From hooks/useEstimate.ts:
```typescript
applyVoiceCommands(commands: VoiceCommand[], sheetIndex?: number): { executed: boolean; routing: ConfidenceResult }
undo(): void
updateMeta(field: keyof Estimate, value: string | number): void
getSheetMargin(sheetIndex: number): number
```

From hooks/useVoice.ts:
```typescript
// recentCommandsRef: useRef<VoiceCommand[]>([])  -- 내부 ref, 외부 접근 불가
// handleModifyResponse: TTS 재생 시 "맞습니까?" 포함 여부는 LLM의 tts_response에 의존
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medium confidence 확인 응답 + useVoice clearLastCommand + 시스템 명령 추가</name>
  <files>hooks/useVoice.ts, components/estimate/EstimateEditor.tsx</files>
  <action>
**A. hooks/useVoice.ts 수정:**

1. `clearLastCommand()` 메서드 추가 — recentCommandsRef에서 마지막 명령 제거. medium confidence 후 "아니" 응답 시 취소된 명령이 컨텍스트에 남지 않도록:
```typescript
const clearLastCommand = useCallback(() => {
  recentCommandsRef.current = recentCommandsRef.current.slice(0, -1)
}, [])
```

2. return 객체에 `clearLastCommand` 추가.

**B. components/estimate/EstimateEditor.tsx 수정:**

1. **pendingConfirmRef 추가:**
```typescript
const pendingConfirmRef = useRef(false)
```

2. **handleVoiceCommands에서 medium confidence 감지:**
applyVoiceCommands 호출 후 routing.level === 'medium'이면 pendingConfirmRef.current = true:
```typescript
const result = applyVoiceCommands(commands, targetSheet)
if (result.executed && result.routing.level === 'medium') {
  pendingConfirmRef.current = true
}
```

3. **onSttText 콜백에서 pendingConfirm 체크 (per D-07):**
"그만" 체크 다음, voiceFlow 체크 이전에 삽입:
```typescript
// medium confidence 확인 응답 체크
if (pendingConfirmRef.current) {
  pendingConfirmRef.current = false
  if (/^(아니|아니오|아니요|틀려|틀렸|달라|다르)/.test(text.trim())) {
    undo()
    voice.clearLastCommand()
    addLog('assistant', '되돌렸습니다.')
    voice.playTts('되돌렸습니다.')
    return  // LLM으로 보내지 않음
  }
  // "네"/"응"/"맞아" 등 긍정 또는 새 명령 → 그대로 진행 (LLM이 처리)
}
```
주의: pendingConfirmRef 체크에서 early return 시 LLM 호출이 자동으로 차단됨 -- useVoice의 processAudio에서 onSttText 콜백이 호출된 후 skipLlm이 false라도, 콜백 내에서 return하면 processAudio의 LLM 호출은 정상 진행된다. 따라서 "아니" 응답 시에도 LLM이 호출되는데, 이를 방지하기 위해:
- onSttText에서 "아니" 감지 시 undo를 실행하되, LLM 결과가 와도 handleVoiceCommands에서 무시하도록 `skipNextCommandRef`를 추가하거나,
- 더 간단한 방법: "아니" 감지 시 바로 undo하고, LLM이 "아니"를 파싱해서 undo 명령을 생성해도 useEstimate.undo()는 이미 스냅샷이 pop된 상태이므로 이전 스냅샷으로 또 undo됨 — 이중 undo 위험.
- **안전한 구현**: pendingConfirm=true 상태에서는 useVoice의 skipLlm을 true로 설정. skipLlm prop을 `flowActive || pendingConfirmRef.current`로 변경:

```typescript
const voice = useVoice({
  // ...
  skipLlm: flowActive || pendingConfirmRef.current,
  // ...
})
```

pendingConfirmRef.current가 true일 때 STT만 수행하고 LLM을 건너뛴다. onSttText에서 "아니" 감지 후 pendingConfirmRef.current = false 설정. 다음 녹음부터 skipLlm=false로 복귀.

**주의**: skipLlm은 useEffect로 ref에 동기화되므로, pendingConfirmRef 변경과 다음 녹음 시작 사이에 렌더가 발생해야 한다. AUTO_RESUME_DELAY 2초 동안 리렌더가 발생하므로 타이밍 안전.

4. **read_summary 시스템 명령 추가 (per VUX-04):**
handleVoiceCommands의 switch문에:
```typescript
case 'read_summary': {
  const sheet = estimate.sheets[activeSheetIndex >= 0 ? activeSheetIndex : 0]
  const total = sheet?.grand_total ?? 0
  const itemCount = sheet?.items.length ?? 0
  const margin = getSheetMargin(activeSheetIndex >= 0 ? activeSheetIndex : 0)
  const formatWon = (v: number) => `${Math.round(v / 10000).toLocaleString()}만원`
  const msg = `${sheet?.type ?? ''} 면적 ${estimate.m2}제곱미터, 공종 ${itemCount}개, 총액 ${formatWon(total)}, 마진 ${Math.round(margin)}퍼센트.`
  addLog('assistant', msg)
  voice.playTts(msg)
  return
}
```

5. **read_margin 시스템 명령 추가:**
```typescript
case 'read_margin': {
  const margin = getSheetMargin(activeSheetIndex >= 0 ? activeSheetIndex : 0)
  const sheetType = estimate.sheets[activeSheetIndex >= 0 ? activeSheetIndex : 0]?.type ?? ''
  const msg = `${sheetType} 마진 ${Math.round(margin)}퍼센트.`
  addLog('assistant', msg)
  voice.playTts(msg)
  return
}
```

6. **update_meta 시스템 명령 추가 (Pitfall 4 대응):**
handleVoiceCommands의 시스템 명령 체크 배열에 'update_meta' 추가:
```typescript
const sysCmd = commands.find(c =>
  ['save', 'email', 'load', 'undo', 'switch_tab', 'read_summary', 'read_margin', 'compare', 'set_margin', 'restore_to', 'update_meta'].includes(c.action)
)
```
switch문에:
```typescript
case 'update_meta': {
  const field = sysCmd.field as keyof Estimate
  if (sysCmd.value !== undefined && field) {
    updateMeta(field, sysCmd.value)
    const msg = `${String(field)} ${sysCmd.value}으로 변경.`
    addLog('assistant', msg)
    voice.playTts(msg)
  }
  return
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>medium confidence 확인 처리가 pendingConfirmRef + skipLlm 연동으로 구현됨. "아니" 응답 시 undo + clearLastCommand 실행. read_summary/read_margin/update_meta 시스템 명령이 handleVoiceCommands에 추가됨. TypeScript 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: VAD 5초 무음 감지를 useVoiceEditMode에 통합</name>
  <files>hooks/useVoiceEditMode.ts</files>
  <action>
useVoiceEditMode.ts에 AnalyserNode 기반 VAD를 통합한다 (per D-05, RESEARCH.md의 VAD 패턴).

1. **UseVoiceEditModeOptions에 VAD 관련 콜백 추가:**
```typescript
interface UseVoiceEditModeOptions {
  voiceStatus: VoiceStatus
  onAutoResume: () => void
  onPlayTts: (text: string) => Promise<void>
  onStopRecording: () => void  // 추가: VAD 무음 감지 시 녹음 중지
}
```

2. **VAD 상수:**
```typescript
const SILENCE_THRESHOLD = -35  // dB
const SILENCE_DURATION = 5000  // 5초 (per D-05)
```

3. **VAD 시작/중지 로직:**
- `startVad(stream: MediaStream)`: AudioContext + AnalyserNode 생성. requestAnimationFrame 루프로 RMS → dB 변환. silenceStart 추적. 5초 연속 무음 시 onStopRecording() + exitEditMode() 호출.
- `stopVad()`: AudioContext.close() + cancellation flag 설정 (per RESEARCH.md Pitfall 5).
- VAD ref: `vadCleanupRef = useRef<(() => void) | null>(null)`

4. **VAD를 녹음 시작/중지와 연동:**
voiceStatus가 'recording'이 되면 VAD 시작이 필요하지만, useVoiceEditMode는 MediaStream에 직접 접근할 수 없다. 대안:
- options에 `getMediaStream?: () => MediaStream | null` 콜백을 추가하는 대신,
- **더 간단한 방법**: `navigator.mediaDevices.getUserMedia({ audio: true })`로 별도 스트림을 가져와서 분석만 한다. useVoice의 녹음 스트림과 별도 — 단, 마이크 권한은 이미 있으므로 즉시 획득 가능.
- **가장 간단한 방법**: VAD를 voiceStatus === 'recording' 시점에 시작하고, voiceStatus !== 'recording'이면 중지. useEffect로:
```typescript
useEffect(() => {
  if (!isEditMode) return
  if (voiceStatus === 'recording') {
    // VAD 시작
    let active = true
    navigator.mediaDevices.getUserMedia({ audio: true }).then(stream => {
      if (!active) { stream.getTracks().forEach(t => t.stop()); return }
      const ctx = new AudioContext()
      const source = ctx.createMediaStreamSource(stream)
      const analyser = ctx.createAnalyser()
      analyser.fftSize = 2048
      source.connect(analyser)
      const dataArray = new Float32Array(analyser.frequencyBinCount)
      let silenceStart: number | null = null

      const check = () => {
        if (!active) return
        analyser.getFloatTimeDomainData(dataArray)
        const rms = Math.sqrt(dataArray.reduce((s, v) => s + v * v, 0) / dataArray.length)
        const db = 20 * Math.log10(Math.max(rms, 1e-10))
        if (db < SILENCE_THRESHOLD) {
          if (!silenceStart) silenceStart = Date.now()
          else if (Date.now() - silenceStart >= SILENCE_DURATION) {
            callbacksRef.current.onStopRecording()
            exitEditMode()
            return
          }
        } else {
          silenceStart = null
        }
        requestAnimationFrame(check)
      }
      check()

      vadCleanupRef.current = () => {
        active = false
        stream.getTracks().forEach(t => t.stop())
        ctx.close().catch(() => {})
      }
    }).catch(() => {})

    return () => {
      active = false
      vadCleanupRef.current?.()
      vadCleanupRef.current = null
    }
  } else {
    // 녹음 아닐 때 VAD 정리
    vadCleanupRef.current?.()
    vadCleanupRef.current = null
  }
}, [isEditMode, voiceStatus])
```

5. **exitEditMode에서 VAD 정리 보장:**
```typescript
const exitEditMode = useCallback(() => {
  setIsEditMode(false)
  // 자동 재개 타이머 정리
  if (resumeTimerRef.current) {
    clearTimeout(resumeTimerRef.current)
    resumeTimerRef.current = null
  }
  // VAD 정리
  vadCleanupRef.current?.()
  vadCleanupRef.current = null
}, [])
```

주의사항 (RESEARCH.md 피트폴):
- VAD의 navigator.mediaDevices.getUserMedia는 두 번째 마이크 스트림 — Android에서 충돌 가능성. 실제 테스트 필요. 문제 시 VAD를 비활성화하는 설정 추가.
- requestAnimationFrame 루프는 `active` flag로 반드시 중단.
- AudioContext.close()는 async — .catch()로 에러 무시.
- VAD는 수정 모드(isEditMode=true) + 녹음 중(voiceStatus==='recording')에서만 작동 (per D-05).
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>VAD가 수정 모드 녹음 중에만 작동하며, 5초 무음(-35dB) 감지 시 녹음 중지 + 수정 모드 해제. AnalyserNode 기반 구현. 빌드 에러 없음. VOICE-07/08 + VUX-04/05 요구사항 완성.</done>
</task>

</tasks>

<verification>
npm run build && npm run lint
빌드+린트 통과 후, medium confidence 확인 로직 + 시스템 명령 + VAD가 코드 수준에서 완성되었는지 확인.
</verification>

<success_criteria>
1. pendingConfirmRef로 medium confidence 확인 응답이 처리되고, "아니" 시 undo + clearLastCommand 실행
2. read_summary/read_margin이 TTS로 현재 상태를 읽어줌
3. update_meta가 handleVoiceCommands에서 처리됨
4. VAD가 수정 모드 녹음 중 5초 무음 시 수정 모드를 자동 종료
5. npm run build, npm run lint 모두 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-edit-loop/02-02-SUMMARY.md`
</output>

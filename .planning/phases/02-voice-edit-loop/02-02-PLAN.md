---
phase: 02-voice-edit-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - hooks/useVoice.ts
  - hooks/useVoiceEditMode.ts
  - hooks/useEstimateVoice.ts
autonomous: true
requirements:
  - VOICE-07
  - VOICE-08
  - VUX-04
  - VUX-05
must_haves:
  truths:
    - '확신도 95%+ 명령은 즉시 실행되고 결과만 TTS로 알린다'
    - '확신도 70-95% 명령은 실행 후 "맞습니까?" TTS가 재생되고, "아니" 응답 시 undo + TTS "되돌렸습니다"'
    - '확신도 70% 미만 명령은 실행되지 않고 되묻기 TTS가 재생된다'
    - '"취소"/"되돌려" 음성 명령으로 직전 스냅샷이 복원된다'
    - '"현재 상태 알려줘"가 TTS로 면적/공종수/총액/마진율을 읽어준다'
    - '"마진 얼마야?"가 TTS로 현재 마진율을 읽어준다'
    - '"면적 200으로 변경" 같은 update_meta 명령이 처리된다'
    - 'VAD 5초 무음 감지로 수정 모드가 자동 종료된다 (feature flag로 비활성화 가능)'
    - 'clarificationCountRef가 수정 모드 종료 시 0으로 리셋된다'
  artifacts:
    - path: "hooks/useVoice.ts"
      provides: "pendingConfirm 상태 관리 + clearLastCommand 메서드 + clarificationCountRef 리셋 콜백"
    - path: "hooks/useVoiceEditMode.ts"
      provides: "VAD 통합 (AnalyserNode 기반 5초 무음 감지, feature flag 제어)"
    - path: "hooks/useEstimateVoice.ts"
      provides: "medium confidence 확인 처리 + read_summary/read_margin/update_meta 시스템 명령"
  key_links:
    - from: "hooks/useEstimateVoice.ts pendingConfirm state"
      to: "hooks/useVoice.ts skipLlm"
      via: "useState reactive update ensures skipLlm is true when pendingConfirm"
      pattern: "pendingConfirm"
    - from: "hooks/useVoiceEditMode.ts VAD onSilence"
      to: "hooks/useVoiceEditMode.ts exitEditMode"
      via: "AnalyserNode silence detection (behind enableVad flag)"
      pattern: "SILENCE_DURATION"
    - from: "useEstimateVoice handleVoiceCommands"
      to: "useEstimate updateMeta"
      via: "update_meta action routing"
      pattern: "update_meta"
    - from: "useVoiceEditMode exitEditMode"
      to: "useVoice resetClarificationCount"
      via: "onExit callback"
      pattern: "clarificationCountRef.*= 0"
---

<objective>
확신도 분기 UX를 완성하고, 시스템 명령(요약/마진/update_meta)을 추가하며, VAD 무음 감지를 구현하여 음성 편집 루프의 모든 요구사항을 충족한다.

Purpose: Plan 02-01에서 수정 모드 진입/종료/자동재개 루프가 완성되었지만, medium confidence 확인 응답 처리, 시스템 명령 일부, VAD가 미구현이다. 이 gap을 채워 Phase 2 전체 요구사항을 완성한다.
Output: 확신도 3단계 분기 완전 동작 + read_summary/read_margin/update_meta + VAD 5초 무음 종료
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-voice-edit-loop/02-CONTEXT.md
@.planning/phases/02-voice-edit-loop/02-RESEARCH.md
@.planning/phases/02-voice-edit-loop/02-01-SUMMARY.md

@hooks/useVoice.ts
@hooks/useEstimate.ts
@hooks/useVoiceEditMode.ts
@hooks/useEstimateVoice.ts
@lib/voice/commands.ts
@lib/voice/confidenceRouter.ts

<interfaces>
<!-- From 02-01 plan outputs -->

From hooks/useVoiceEditMode.ts (created in 02-01):
```typescript
export function useVoiceEditMode(options: UseVoiceEditModeOptions): {
  isEditMode: boolean
  enterEditMode: () => Promise<void>
  exitEditMode: () => void
}
```

From hooks/useEstimateVoice.ts (created in 02-01):
```typescript
// Contains handleVoiceCommands, voice, voiceFlow, editMode
// handleVoiceCommands has system command switch
```

From hooks/useEstimate.ts:
```typescript
applyVoiceCommands(commands: VoiceCommand[], sheetIndex?: number): { executed: boolean; routing: ConfidenceResult }
undo(): void
updateMeta(field: keyof Estimate, value: string | number): void
getSheetMargin(sheetIndex: number): number
```

From hooks/useVoice.ts:
```typescript
// recentCommandsRef: useRef<VoiceCommand[]>([])  -- 내부 ref, 외부 접근 불가
// handleModifyResponse: TTS 재생 시 "맞습니까?" 포함 여부는 LLM의 tts_response에 의존
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medium confidence 확인 응답 (pendingConfirm useState) + useVoice clearLastCommand/resetClarification + 시스템 명령 추가</name>
  <files>hooks/useVoice.ts, hooks/useEstimateVoice.ts</files>
  <action>
**A. hooks/useVoice.ts 수정:**

1. `clearLastCommand()` 메서드 추가 -- recentCommandsRef에서 마지막 명령 제거:
```typescript
const clearLastCommand = useCallback(() => {
  recentCommandsRef.current = recentCommandsRef.current.slice(0, -1)
}, [])
```

2. `resetClarificationCount()` 메서드 추가 -- 수정 모드 종료 시 되묻기 카운터 리셋 (per WARNING 3, VOICE-08):
```typescript
const resetClarificationCount = useCallback(() => {
  clarificationCountRef.current = 0
}, [])
```

3. return 객체에 `clearLastCommand`, `resetClarificationCount` 추가.

**B. hooks/useEstimateVoice.ts 수정:**

1. **pendingConfirm을 useState로 관리 (per WARNING 1):**
```typescript
const [pendingConfirm, setPendingConfirm] = useState(false)
```
useRef가 아닌 useState를 사용하는 이유: pendingConfirm이 true일 때 skipLlm도 true여야 하는데, ref 변경은 리렌더를 트리거하지 않아 skipLlm이 stale할 수 있음. TTS가 짧으면 사용자가 다음 render cycle 전에 "아니"를 말할 수 있어 skipLlm이 여전히 false인 상태로 LLM이 호출됨. useState는 리렌더를 트리거하여 skipLlm이 항상 최신값을 가짐.

2. **useVoice의 skipLlm을 pendingConfirm과 연동:**
```typescript
const voice = useVoice({
  // ...
  skipLlm: flowActive || pendingConfirm,
  // ...
})
```

3. **handleVoiceCommands에서 medium confidence 감지:**
applyVoiceCommands 호출 후 routing.level === 'medium'이면 setPendingConfirm(true):
```typescript
const result = applyVoiceCommands(commands, targetSheet)
if (result.executed && result.routing.level === 'medium') {
  setPendingConfirm(true)
}
```

4. **onSttText 콜백에서 pendingConfirm 체크 (per D-07):**
"그만" 체크 다음, voiceFlow 체크 이전에 삽입:
```typescript
// medium confidence 확인 응답 체크
if (pendingConfirm) {
  setPendingConfirm(false)
  if (/^(아니|아니오|아니요|틀려|틀렸|달라|다르)/.test(text.trim())) {
    undo()
    voice.clearLastCommand()
    addLog('assistant', '되돌렸습니다.')
    voice.playTts('되돌렸습니다.')
    return  // LLM으로 보내지 않음 (skipLlm=true이므로 processAudio에서도 LLM 건너뜀)
  }
  // "네"/"응"/"맞아" 등 긍정 또는 새 명령 -> 그대로 진행
}
```

skipLlm이 `flowActive || pendingConfirm`으로 설정되므로, pendingConfirm=true일 때 STT만 수행하고 LLM을 건너뛴다. setPendingConfirm(false) 호출 시 리렌더가 발생하여 skipLlm=false로 복귀. 다음 녹음(AUTO_RESUME_DELAY 2초 후)에서는 LLM이 정상 호출된다.

5. **exitEditMode 시 clarificationCountRef 리셋 (per WARNING 3, VOICE-08):**
useVoiceEditMode의 options에 `onExit` 콜백을 추가하거나, useEstimateVoice에서 editMode.exitEditMode 호출 시 voice.resetClarificationCount()도 호출하도록 래퍼 함수 생성:
```typescript
const handleExitEditMode = useCallback(() => {
  editMode.exitEditMode()
  voice.resetClarificationCount()
  setPendingConfirm(false)  // 확인 대기 상태도 초기화
}, [editMode, voice])
```
이 래퍼를 "그만" 감지, VAD 종료, 기타 수정 모드 종료 시점에서 사용.

6. **read_summary 시스템 명령 추가 (per VUX-04):**
handleVoiceCommands의 switch문에:
```typescript
case 'read_summary': {
  const sheet = estimate.sheets[activeSheetIndex >= 0 ? activeSheetIndex : 0]
  const total = sheet?.grand_total ?? 0
  const itemCount = sheet?.items.length ?? 0
  const margin = getSheetMargin(activeSheetIndex >= 0 ? activeSheetIndex : 0)
  const formatWon = (v: number) => `${Math.round(v / 10000).toLocaleString()}만원`
  const msg = `${sheet?.type ?? ''} 면적 ${estimate.m2}제곱미터, 공종 ${itemCount}개, 총액 ${formatWon(total)}, 마진 ${Math.round(margin)}퍼센트.`
  addLog('assistant', msg)
  voice.playTts(msg)
  return
}
```

7. **read_margin 시스템 명령 추가:**
```typescript
case 'read_margin': {
  const margin = getSheetMargin(activeSheetIndex >= 0 ? activeSheetIndex : 0)
  const sheetType = estimate.sheets[activeSheetIndex >= 0 ? activeSheetIndex : 0]?.type ?? ''
  const msg = `${sheetType} 마진 ${Math.round(margin)}퍼센트.`
  addLog('assistant', msg)
  voice.playTts(msg)
  return
}
```

8. **update_meta 시스템 명령 추가 (Pitfall 4 대응):**
handleVoiceCommands의 시스템 명령 체크 배열에 'update_meta' 추가하고 switch문에:
```typescript
case 'update_meta': {
  const field = sysCmd.field as keyof Estimate
  if (sysCmd.value !== undefined && field) {
    updateMeta(field, sysCmd.value)
    const msg = `${String(field)} ${sysCmd.value}으로 변경.`
    addLog('assistant', msg)
    voice.playTts(msg)
  }
  return
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>pendingConfirm이 useState로 관리되어 skipLlm이 reactive하게 동작. "아니" 응답 시 undo + clearLastCommand 실행. read_summary/read_margin/update_meta 시스템 명령이 추가됨. clarificationCountRef가 수정 모드 종료 시 리셋됨. TypeScript 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: VAD 5초 무음 감지를 useVoiceEditMode에 통합 (feature flag 포함)</name>
  <files>hooks/useVoiceEditMode.ts</files>
  <action>
useVoiceEditMode.ts에 AnalyserNode 기반 VAD를 통합한다 (per D-05, RESEARCH.md의 VAD 패턴).

1. **UseVoiceEditModeOptions에 VAD 관련 옵션 추가:**
```typescript
interface UseVoiceEditModeOptions {
  voiceStatus: VoiceStatus
  onAutoResume: () => void
  onPlayTts: (text: string) => Promise<void>
  onStopRecording: () => void  // VAD 무음 감지 시 녹음 중지
  enableVad?: boolean  // VAD feature flag (per WARNING 2). 기본값 true.
}
```

2. **VAD 상수:**
```typescript
const SILENCE_THRESHOLD = -35  // dB
const SILENCE_DURATION = 5000  // 5초 (per D-05)
```

3. **VAD feature flag (per WARNING 2 -- 갤럭시탭 dual-stream 위험):**
`enableVad` 옵션으로 VAD를 비활성화할 수 있다. 갤럭시탭에서 VAD + 녹음 동시 동작에 문제가 있으면 false로 설정. 기본값은 true이되, 실제 useEstimateVoice에서 호출 시 갤럭시탭 테스트 결과에 따라 조정 가능.

4. **VAD 시작/중지 로직:**
voiceStatus가 'recording'이 되면 VAD 시작. voiceStatus !== 'recording'이면 중지.
navigator.mediaDevices.getUserMedia로 별도 스트림을 가져와서 AnalyserNode로 분석만 한다.

```typescript
useEffect(() => {
  if (!isEditMode || !enableVad) return
  if (voiceStatus === 'recording') {
    let active = true
    navigator.mediaDevices.getUserMedia({ audio: true }).then(stream => {
      if (!active) { stream.getTracks().forEach(t => t.stop()); return }
      const ctx = new AudioContext()
      const source = ctx.createMediaStreamSource(stream)
      const analyser = ctx.createAnalyser()
      analyser.fftSize = 2048
      source.connect(analyser)
      const dataArray = new Float32Array(analyser.frequencyBinCount)
      let silenceStart: number | null = null

      const check = () => {
        if (!active) return
        analyser.getFloatTimeDomainData(dataArray)
        const rms = Math.sqrt(dataArray.reduce((s, v) => s + v * v, 0) / dataArray.length)
        const db = 20 * Math.log10(Math.max(rms, 1e-10))
        if (db < SILENCE_THRESHOLD) {
          if (!silenceStart) silenceStart = Date.now()
          else if (Date.now() - silenceStart >= SILENCE_DURATION) {
            callbacksRef.current.onStopRecording()
            exitEditMode()
            return
          }
        } else {
          silenceStart = null
        }
        requestAnimationFrame(check)
      }
      check()

      vadCleanupRef.current = () => {
        active = false
        stream.getTracks().forEach(t => t.stop())
        ctx.close().catch(() => {})
      }
    }).catch(() => {})

    return () => {
      active = false
      vadCleanupRef.current?.()
      vadCleanupRef.current = null
    }
  } else {
    vadCleanupRef.current?.()
    vadCleanupRef.current = null
  }
}, [isEditMode, voiceStatus, enableVad])
```

5. **exitEditMode에서 VAD 정리 보장:**
```typescript
const exitEditMode = useCallback(() => {
  setIsEditMode(false)
  if (resumeTimerRef.current) {
    clearTimeout(resumeTimerRef.current)
    resumeTimerRef.current = null
  }
  vadCleanupRef.current?.()
  vadCleanupRef.current = null
}, [])
```

주의사항 (WARNING 2 대응):
- VAD의 navigator.mediaDevices.getUserMedia는 두 번째 마이크 스트림. 갤럭시탭에서 충돌 가능성이 있으므로 enableVad feature flag로 제어.
- requestAnimationFrame 루프는 `active` flag로 반드시 중단.
- AudioContext.close()는 async -- .catch()로 에러 무시.
- VAD는 수정 모드(isEditMode=true) + enableVad=true + 녹음 중(voiceStatus==='recording')에서만 작동.
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>VAD가 수정 모드 녹음 중에만 작동하며, 5초 무음(-35dB) 감지 시 녹음 중지 + 수정 모드 해제. enableVad feature flag로 비활성화 가능. 갤럭시탭에서 VAD + 녹음 동시 동작 확인이 done criteria에 포함됨. 빌드 에러 없음. VOICE-07/08 + VUX-04/05 요구사항 완성.</done>
</task>

</tasks>

<verification>
npm run build && npm run lint
빌드+린트 통과 후, medium confidence 확인 로직 + 시스템 명령 + VAD가 코드 수준에서 완성되었는지 확인.
</verification>

<success_criteria>
1. pendingConfirm이 useState로 관리되어 skipLlm이 reactive하게 연동 (WARNING 1 해결)
2. read_summary/read_margin이 TTS로 현재 상태를 읽어줌
3. update_meta가 handleVoiceCommands에서 처리됨
4. VAD가 수정 모드 녹음 중 5초 무음 시 수정 모드를 자동 종료, enableVad flag로 비활성화 가능 (WARNING 2 해결)
5. clarificationCountRef가 exitEditMode 시 0으로 리셋됨 (WARNING 3 해결)
6. npm run build, npm run lint 모두 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-edit-loop/02-02-SUMMARY.md`
</output>

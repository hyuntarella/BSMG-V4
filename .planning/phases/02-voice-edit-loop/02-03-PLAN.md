---
phase: 02-voice-edit-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - components/estimate/EstimateEditor.tsx
autonomous: false
requirements:
  - VOICE-03
  - VOICE-04
  - VOICE-07
  - VUX-02
  - VUX-05
must_haves:
  truths:
    - '수정 모드 진입 -> 음성 수정 -> TTS 피드백 -> 자동 재개 -> 종료 전체 루프가 실제 동작한다'
    - '확신도 분기(high/medium/low)가 실제 음성으로 구분되어 동작한다'
    - '"취소" 음성으로 직전 명령이 복원된다'
  artifacts:
    - path: "components/estimate/EstimateEditor.tsx"
      provides: "Phase 2 전체 배선 완료 + 빌드/린트 통과"
  key_links:
    - from: "useWakeWord 수정 keyword"
      to: "useVoiceEditMode enterEditMode"
      via: "EstimateEditor onEditMode callback"
      pattern: "수정.*enterEditMode"
---

<objective>
Phase 2 전체 구현의 빌드/린트 최종 검증과 사용자 수동 테스트를 위한 체크포인트.

Purpose: Plan 02-01 + 02-02의 배선이 완료된 후, 실제 빌드가 통과하고 음성 편집 루프가 end-to-end로 동작하는지 확인한다.
Output: 빌드/린트 클린 + 사용자 음성 테스트 확인
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-voice-edit-loop/02-01-SUMMARY.md
@.planning/phases/02-voice-edit-loop/02-02-SUMMARY.md
@components/estimate/EstimateEditor.tsx
@hooks/useVoiceEditMode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 빌드/린트 최종 검증 + 코드 정리</name>
  <files>components/estimate/EstimateEditor.tsx</files>
  <action>
1. `npm run build && npm run lint` 실행.
2. 에러가 있으면 수정:
   - TypeScript 타입 에러 -> 타입 수정
   - ESLint 경고 -> exhaustive-deps 등 수정
   - 빌드 에러 -> import/export 수정
3. EstimateEditor.tsx 줄 수 확인. 450줄 이상이면 음성 관련 로직을 정리:
   - handleVoiceCommands의 시스템 명령 switch문이 길면, 별도 함수 `handleSystemCommand`로 추출 (같은 파일 내)
   - 긴 JSX 블록은 컴포넌트로 분리하지 않되, 불필요한 주석이나 빈 줄을 정리
4. `npm run build && npm run lint` 재실행하여 클린 확인.
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build + npm run lint 모두 에러/경고 없이 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 음성 편집 루프 E2E 수동 테스트</name>
  <files>components/estimate/EstimateEditor.tsx</files>
  <action>
사용자가 갤럭시탭에서 실제 음성으로 8개 시나리오를 테스트한다.

Phase 2에서 구현된 기능:
- "수정" 웨이크워드로 수정 모드 진입
- 음성으로 단가 변경/공종 추가/삭제/일괄 조정/총액 역산
- 확신도 3단계 분기 (high: 즉시실행, medium: 실행+확인, low: 되묻기)
- TTS 완료 2초 후 자동 녹음 재개 (연속 수정)
- "그만"/"종료" 또는 5초 무음으로 수정 모드 종료
- "취소"로 직전 명령 undo
- "현재 상태 알려줘", "마진 얼마야?" 시스템 명령

테스트 시나리오:
1. 수정 모드 진입: "수정" -> TTS "수정 모드입니다. 말씀하세요." + 하단 표시
2. 단가 수정: "바탕정리 재료비 400원으로" -> 테이블 반영 + TTS 피드백
3. 자동 재개: TTS 끝난 후 2초 뒤 녹음 자동 시작
4. 공종 추가: "크랙보수 20미터 추가" -> 행 추가
5. 컨텍스트 참조: "그거 빼줘" -> 크랙보수 삭제
6. 취소: "취소" -> 삭제 복원
7. 시스템 명령: "현재 상태 알려줘" -> TTS 요약
8. 종료: "그만" -> 수정 모드 해제
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>사용자가 8개 시나리오를 테스트하고 "approved" 응답</done>
</task>

</tasks>

<verification>
npm run build && npm run lint 통과 + 사용자 음성 테스트 8개 시나리오 확인
</verification>

<success_criteria>
1. 빌드/린트 완전 클린
2. 사용자가 8개 시나리오를 순서대로 테스트하고 "approved" 응답
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-edit-loop/02-03-SUMMARY.md`
</output>

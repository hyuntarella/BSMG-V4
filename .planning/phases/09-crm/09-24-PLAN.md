---
phase: 09-crm
plan: 24
type: execute
wave: 2
depends_on: [23]
files_modified:
  - app/(authenticated)/crm/page.tsx
  - components/crm/KanbanBoard.tsx
  - components/crm/KanbanCard.tsx
  - hooks/useCrm.ts
autonomous: true
requirements: [CRM-24]
must_haves:
  truths:
    - "CRM 페이지 접속 시 Notion 레코드가 칸반보드로 표시된다"
    - "5개 탭(문의/영업/장기/시공/하자) 전환이 동작한다"
    - "각 탭에서 해당 stage의 파이프라인이 컬럼으로 표시된다"
    - "각 카드에 주소, 전화번호, 시공평수, 담당자, 견적금액이 보인다"
  artifacts:
    - path: "app/(authenticated)/crm/page.tsx"
      provides: "CRM 서버 컴포넌트 (SSR 데이터 로드)"
    - path: "components/crm/KanbanBoard.tsx"
      provides: "5탭 칸반보드 레이아웃"
    - path: "components/crm/KanbanCard.tsx"
      provides: "칸반 카드 UI"
    - path: "hooks/useCrm.ts"
      provides: "CRM 클라이언트 상태 관리"
  key_links:
    - from: "app/(authenticated)/crm/page.tsx"
      to: "/api/crm"
      via: "server-side fetch"
    - from: "components/crm/KanbanBoard.tsx"
      to: "lib/notion/types.ts"
      via: "CrmRecord, STAGE_MAP imports"
---

<objective>
CRM 칸반보드 UI 구축 — Notion 데이터를 5탭 칸반보드로 표시.

Purpose: GAS CRM.html의 칸반보드를 Next.js로 완전 재구현. 5개 단계 탭 + 파이프라인 컬럼 레이아웃.
Output: 작동하는 칸반보드 페이지
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-crm/09-23-SUMMARY.md

Interfaces from Plan 23:
```typescript
// lib/notion/types.ts
export interface CrmRecord {
  id: string;
  address: string;
  customerName: string | null;
  phone: string | null;
  email: string | null;
  manager: string | null;
  stage: string | null;
  pipeline: string | null;
  contractStatus: string | null;
  inquiryChannel: string | null;
  workTypes: string[];
  estimateAmount: number | null;
  contractAmount: number | null;
  deposit: number | null;
  balance: number | null;
  area: string | null;
  memo: string | null;
  inquiryDate: string | null;
  visitDate: string | null;
  balanceCompleteDate: string | null;
  estimateSentDate: string | null;
  estimateViewedDate: string | null;
  driveUrl: string | null;
  estimateWebUrl: string | null;
  createdTime: string;
  lastEditedTime: string;
}

export const STAGE_MAP: Record<string, string[]>;
export const PIPELINE_TO_STAGE: Record<string, string>;
```

기존 UI 패턴:
- Header: components/layout/Header.tsx (sticky top, hamburger menu)
- 페이지 레이아웃: min-h-screen bg-bg, Header 포함
- Tailwind 사용, 인라인 스타일 금지
</context>

<tasks>

<task type="auto">
  <name>Task 1: CRM 페이지 SSR + useCrm 훅</name>
  <files>app/(authenticated)/crm/page.tsx, hooks/useCrm.ts</files>
  <action>
1. hooks/useCrm.ts 생성 (클라이언트 훅):
   - records: CrmRecord[] 상태
   - loading: boolean
   - error: string | null
   - activeStage: string (기본값 '0.문의')
   - searchQuery: string
   - managerFilter: string (기본값 '전체')
   - fetchRecords(): GET /api/crm → setRecords
   - filteredRecords: useMemo로 activeStage에 해당하는 레코드만 필터 (PIPELINE_TO_STAGE[record.pipeline] === activeStage). searchQuery로 address/customerName/phone 검색. managerFilter로 담당자 필터.
   - updateRecordLocal(id, partial): 로컬 records 상태 업데이트 (낙관적 UI)
   - setActiveStage, setSearchQuery, setManagerFilter
   - initialRecords prop으로 SSR 데이터 받기 (useState 초기값)

2. app/(authenticated)/crm/page.tsx 수정:
   - 서버 컴포넌트로 유지. 내부에서 /api/crm를 서버사이드 fetch (절대 URL 사용: `${process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'}/api/crm`). 또는 lib/notion/crm.ts의 getAllRecords()를 직접 서버에서 호출 (더 효율적).
   - 실제로는 getAllRecords()를 직접 import해서 호출하는 것이 API route 거치는 것보다 빠름.
   - CrmPageClient 컴포넌트에 records를 props로 전달.
   - CrmPageClient는 'use client' — useCrm 훅 사용, KanbanBoard 렌더링.
   - Header 포함, 전체 레이아웃: min-h-screen bg-gray-50.
  </action>
  <verify>
    <automated>npx tsc --noEmit hooks/useCrm.ts app/\(authenticated\)/crm/page.tsx</automated>
  </verify>
  <done>CRM 페이지가 SSR로 Notion 데이터를 로드하고 클라이언트에 전달</done>
</task>

<task type="auto">
  <name>Task 2: KanbanBoard + KanbanCard 컴포넌트</name>
  <files>components/crm/KanbanBoard.tsx, components/crm/KanbanCard.tsx</files>
  <action>
1. components/crm/KanbanBoard.tsx ('use client'):
   - Props: records: CrmRecord[], activeStage, onStageChange, onCardClick(record)
   - 상단 탭 바: 5개 탭 버튼 (문의/영업/장기/시공/하자). activeStage와 일치하는 탭 강조 (bg-brand text-white). 탭은 가로 스크롤 가능 (overflow-x-auto).
   - 탭 아래: STAGE_MAP[activeStage]의 파이프라인들을 가로 컬럼으로 배치.
   - 각 컬럼: 파이프라인명 헤더 + 해당 파이프라인의 카드 목록 (세로 스크롤).
   - 컬럼 레이아웃: flex flex-row gap-3, 각 컬럼 min-w-[260px] max-w-[300px], 가로 스크롤 (overflow-x-auto).
   - 각 컬럼 헤더: 파이프라인명 + 카드 수 뱃지.
   - 빈 컬럼: "레코드 없음" 텍스트.

2. components/crm/KanbanCard.tsx:
   - Props: record: CrmRecord, onClick: () => void
   - 카드 디자인: bg-white rounded-lg shadow-sm border p-3 cursor-pointer hover:shadow-md transition-shadow.
   - 내용:
     - 주소 (font-medium text-sm, 1줄 truncate)
     - 고객명 + 전화번호 (text-xs text-gray-500, 같은 줄)
     - 시공평수 (있으면 표시)
     - 담당자 칩 (이창엽=blue, 박민우=green, 미배정=gray, 작은 rounded pill)
     - 문의채널 (text-xs)
     - 견적금액 (있으면, 만원 단위 포맷, font-semibold)
   - 전화번호 클릭 시 tel: 링크 (이벤트 버블링 방지 e.stopPropagation)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>5탭 칸반보드가 렌더링되고, 탭 전환 시 해당 stage의 파이프라인 컬럼만 표시되며, 각 카드에 주소/전화번호/담당자/견적금액이 보인다</done>
</task>

</tasks>

<verification>
npm run build 통과.
/crm 페이지 접속 시 칸반보드 렌더링.
탭 클릭 시 해당 stage의 파이프라인 컬럼으로 전환.
</verification>

<success_criteria>
- CRM 페이지가 SSR로 Notion 데이터를 로드하여 칸반보드 렌더링
- 5개 탭 전환이 동작하고 각 탭별 파이프라인 컬럼이 올바르게 표시
- 카드에 주요 정보(주소, 전화번호, 담당자, 견적금액) 표시
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm/09-24-SUMMARY.md`
</output>

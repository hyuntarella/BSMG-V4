---
phase: 09-crm
plan: 27
type: execute
wave: 3
depends_on: [24]
files_modified:
  - components/crm/PerformanceTab.tsx
  - components/crm/PerformanceCard.tsx
  - components/crm/KanbanBoard.tsx
autonomous: true
requirements: [CRM-27]
must_haves:
  truths:
    - "실적 탭에서 계약성공/실패 카드가 갤러리로 표시된다"
    - "카드가 연도 > 월 섹션으로 그룹핑된다"
    - "성공 카드는 파란 배경, 실패 카드는 붉은 배경이다"
    - "검색바와 담당자 필터가 동작한다"
    - "스크롤 시 연도/월 섹션 헤더가 sticky 유지된다"
  artifacts:
    - path: "components/crm/PerformanceTab.tsx"
      provides: "실적 갤러리 뷰"
    - path: "components/crm/PerformanceCard.tsx"
      provides: "실적 카드 컴포넌트"
  key_links:
    - from: "components/crm/PerformanceTab.tsx"
      to: "hooks/useCrm.ts"
      via: "records 필터링 (계약상태로)"
---

<objective>
CRM 실적 탭 — 계약성공/실패를 카드 갤러리로 월별/연도별 그룹핑 표시.

Purpose: 영업 실적을 시각적으로 확인하는 갤러리 뷰. 사진첩처럼 스크롤하며 과거 계약을 둘러본다.
Output: 실적 탭 + 카드 갤러리 + 검색/필터
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-crm/09-24-SUMMARY.md

Interfaces:
```typescript
export interface CrmRecord {
  contractStatus: string | null;  // '계약성공' | '계약실패' | null 등
  address: string;
  customerName: string | null;
  manager: string | null;
  estimateAmount: number | null;
  contractAmount: number | null;
  workTypes: string[];
  area: string | null;
  inquiryDate: string | null;  // ISO date string
  // ...
}
```

실적 필터링 기준:
- 계약성공: contractStatus === '계약성공' OR pipeline이 '잔금완료'인 레코드
- 계약실패: contractStatus === '계약실패' OR pipeline이 '재연락금지'인 레코드
- 그룹핑 기준 날짜: inquiryDate (문의일자) — 없으면 createdTime 사용
</context>

<tasks>

<task type="auto">
  <name>Task 1: PerformanceTab + PerformanceCard 컴포넌트</name>
  <files>components/crm/PerformanceTab.tsx, components/crm/PerformanceCard.tsx</files>
  <action>
1. components/crm/PerformanceCard.tsx ('use client'):
   - Props: record: CrmRecord, isSuccess: boolean, onClick: () => void
   - 카드 디자인:
     - 성공: bg-blue-50 border-blue-200 border rounded-lg p-3
     - 실패: bg-red-50 border-red-200 border rounded-lg p-3
   - 내용:
     - 주소 (font-medium text-sm, 1줄 truncate)
     - 고객명 (text-xs)
     - 시공분야 (workTypes.join(', '), text-xs text-gray-500)
     - 시공평수 (area, text-xs)
     - 금액: contractAmount || estimateAmount (만원 단위 포맷, font-semibold)
     - 담당자 칩 (이창엽=blue, 박민우=green)
   - cursor-pointer, hover:shadow-md transition-shadow.

2. components/crm/PerformanceTab.tsx ('use client'):
   - Props: records: CrmRecord[], onCardClick: (record) => void
   - 상단: 검색바 (input) + 담당자 필터 드롭다운 (전체/이창엽/박민우).
   - 실적 레코드 필터링:
     - isSuccess: contractStatus === '계약성공' || pipeline === '잔금완료'
     - isFail: contractStatus === '계약실패' || pipeline === '재연락금지'
     - performanceRecords = records.filter(r => isSuccess(r) || isFail(r))
   - 검색 필터: address, customerName에 searchQuery 포함.
   - 담당자 필터: manager 일치.
   - 그룹핑 로직:
     - 날짜 기준: inquiryDate || createdTime.
     - 연도별 → 월별 정렬 (최신 먼저).
     - 구조: Map<year, Map<month, CrmRecord[]>>
   - 렌더링:
     - 연도 헤더: sticky top-0 z-10 bg-gray-100 px-4 py-2 text-lg font-bold. (예: "2026년")
     - 월 헤더: sticky top-10 z-[9] bg-gray-50 px-4 py-1.5 text-sm font-semibold text-gray-600. (예: "3월")
     - 카드 그리드: grid grid-cols-2 sm:grid-cols-3 gap-3 px-4 pb-4.
     - 각 카드: PerformanceCard.
   - 빈 상태: "실적 데이터가 없습니다" 표시.
   - 통계 요약 (상단): 전체 {N}건, 성공 {N}건, 실패 {N}건, 성공률 {N}%.
  </action>
  <verify>
    <automated>npx tsc --noEmit components/crm/PerformanceTab.tsx components/crm/PerformanceCard.tsx</automated>
  </verify>
  <done>실적 탭에서 계약성공/실패 카드가 연도>월 그룹으로 표시되고 검색/필터 동작</done>
</task>

<task type="auto">
  <name>Task 2: 칸반보드에 실적 탭 통합</name>
  <files>components/crm/KanbanBoard.tsx</files>
  <action>
1. KanbanBoard.tsx 탭에 "실적" 탭 추가:
   - 기존 5개 탭 뒤에 "실적" 탭 추가 (총 6개).
   - activeStage === '실적'일 때 칸반 컬럼 대신 PerformanceTab 렌더링.
   - PerformanceTab에 records 전체를 전달 (실적 탭 내부에서 필터링).
   - onCardClick: 상세 모달 열기 (DetailModal 재사용).

2. 탭 바 디자인:
   - "실적" 탭은 다른 탭과 동일한 스타일. 아이콘이나 특별한 구분 없이 텍스트만.
   - 탭 목록: ['0.문의', '1.영업', '1-1.장기', '2.시공', '3.하자', '실적']
   - 탭 표시명: { '0.문의': '문의', '1.영업': '영업', '1-1.장기': '장기', '2.시공': '시공', '3.하자': '하자', '실적': '실적' }
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>"실적" 탭 클릭 시 갤러리 뷰가 표시되고, 칸반 탭과 매끄럽게 전환된다</done>
</task>

</tasks>

<verification>
npm run build 통과.
실적 탭에서 연도/월별 그룹핑된 카드 갤러리 표시.
성공=파란, 실패=붉은 배경 구분.
검색 + 담당자 필터 동작.
스크롤 시 연도/월 헤더 sticky.
</verification>

<success_criteria>
- 실적 탭에서 계약성공(파란)/실패(붉은) 카드가 갤러리로 표시
- 연도 > 월 그룹핑 + sticky 헤더
- 검색바(주소/고객명) + 담당자 필터 동작
- 통계 요약 (전체/성공/실패/성공률) 상단 표시
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm/09-27-SUMMARY.md`
</output>

---
phase: 09-crm
plan: 28
type: execute
wave: 4
depends_on: [25, 26, 27]
files_modified:
  - components/crm/DetailModal.tsx
  - components/crm/KanbanBoard.tsx
  - hooks/useCrm.ts
  - middleware.ts
  - e2e/crm.spec.ts
autonomous: true
requirements: [CRM-28]
must_haves:
  truths:
    - "상세 모달에서 '견적서 작성' 버튼 클릭 시 /estimate/new으로 이동한다"
    - "상세 모달에서 '제안서 작성' 버튼 클릭 시 /proposal로 이동한다"
    - "칸반보드 상단 검색바로 주소/고객명/전화번호 실시간 필터링 된다"
    - "담당자 필터 드롭다운으로 담당자별 필터링 된다"
    - "Playwright 테스트가 통과한다"
  artifacts:
    - path: "components/crm/DetailModal.tsx"
      provides: "견적서/제안서 작성 연결 버튼"
    - path: "e2e/crm.spec.ts"
      provides: "CRM E2E 테스트"
  key_links:
    - from: "components/crm/DetailModal.tsx"
      to: "/estimate/new"
      via: "router.push with query params"
    - from: "components/crm/KanbanBoard.tsx"
      to: "hooks/useCrm.ts"
      via: "searchQuery, managerFilter state"
---

<objective>
CRM 연결 + 검색 + 필터 + E2E 테스트 — 견적서/제안서 연결, 실시간 검색, 담당자 필터, Playwright 테스트.

Purpose: CRM에서 견적서/제안서로의 워크플로우 연결 완성 + 검색/필터 UX + 테스트 안전망.
Output: 연결 버튼 + 검색/필터 + E2E 테스트
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-crm/09-24-SUMMARY.md
@.planning/phases/09-crm/09-25-SUMMARY.md
@.planning/phases/09-crm/09-26-SUMMARY.md

Interfaces:
```typescript
// hooks/useCrm.ts
searchQuery: string;
managerFilter: string;
setSearchQuery(q: string): void;
setManagerFilter(m: string): void;
filteredRecords: CrmRecord[];  // already filtered by stage + search + manager

// DetailModal props
record: CrmRecord;
onClose: () => void;
onUpdate: (id: string, partial: Partial<CrmRecord>) => void;
```

미들웨어 AUTH 우회 (Playwright 테스트용):
- 환경변수 TEST_MODE=true일 때 미들웨어에서 인증 검사 skip.
- Playwright에서 .env.test에 TEST_MODE=true 설정.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 견적서/제안서 연결 버튼 + 칸반 검색/필터 UI</name>
  <files>components/crm/DetailModal.tsx, components/crm/KanbanBoard.tsx, hooks/useCrm.ts</files>
  <action>
1. DetailModal.tsx에 연결 버튼 추가:
   - 모달 하단 (ActionButtons 위 또는 헤더 영역)에 두 버튼:
     a. "견적서 작성" 버튼 (bg-brand text-white):
        - 클릭 시: router.push(`/estimate/new?crmId=${record.id}&address=${encodeURIComponent(record.address)}&customerName=${encodeURIComponent(record.customerName || '')}&manager=${encodeURIComponent(record.manager || '')}`)
        - 아이콘: 문서 아이콘 (SVG inline)
     b. "제안서 작성" 버튼 (bg-gray-700 text-white):
        - 클릭 시: router.push(`/proposal?address=${encodeURIComponent(record.address)}&manager=${encodeURIComponent(record.manager || '')}`)
        - 아이콘: 파일 아이콘 (SVG inline)
   - 두 버튼은 같은 줄에 flex gap-2.
   - router는 next/navigation의 useRouter 사용.

2. KanbanBoard.tsx 상단에 검색바 + 담당자 필터 추가:
   - 탭 바 위 또는 아래에 검색/필터 바.
   - 검색 input: placeholder="주소, 고객명, 전화번호 검색", 좌측 돋보기 아이콘.
     - onChange: setSearchQuery(e.target.value). 디바운스 불필요 (클라이언트 필터링).
   - 담당자 select: options=['전체', '이창엽', '박민우', '미배정'].
     - onChange: setManagerFilter(e.target.value).
   - 레이아웃: flex gap-2, 검색 flex-1, 담당자 w-28.

3. hooks/useCrm.ts 필터링 로직 확인/수정:
   - filteredRecords useMemo에서:
     - activeStage가 '실적'이면 전체 records 반환 (PerformanceTab 내부에서 필터링).
     - 그 외: stage 필터 + searchQuery 필터 (address, customerName, phone에서 검색) + managerFilter 필터.
   - searchQuery가 빈 문자열이면 필터 skip.
   - managerFilter가 '전체'이면 필터 skip.
  </action>
  <verify>
    <automated>npx tsc --noEmit components/crm/DetailModal.tsx components/crm/KanbanBoard.tsx hooks/useCrm.ts</automated>
  </verify>
  <done>견적서/제안서 연결 버튼 동작, 검색바로 실시간 필터링, 담당자 필터 동작</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E 테스트 + TEST_MODE 미들웨어</name>
  <files>middleware.ts, e2e/crm.spec.ts</files>
  <action>
1. middleware.ts 수정 — TEST_MODE 우회 추가:
   - updateSession 함수 내부, user 체크 전에:
     ```typescript
     // Playwright 테스트 모드: 인증 우회
     if (process.env.TEST_MODE === 'true') {
       return supabaseResponse
     }
     ```
   - 기존 로직은 그대로 유지.

2. Playwright 설치 확인:
   - package.json에 @playwright/test가 없으면 설치: npm install -D @playwright/test
   - playwright.config.ts 생성 (없으면):
     ```typescript
     import { defineConfig } from '@playwright/test'
     export default defineConfig({
       testDir: './e2e',
       use: {
         baseURL: 'http://localhost:3000',
       },
       webServer: {
         command: 'npm run dev',
         port: 3000,
         reuseExistingServer: true,
         env: { TEST_MODE: 'true' },
       },
     })
     ```

3. e2e/crm.spec.ts 생성:
   ```typescript
   import { test, expect } from '@playwright/test'

   test.describe('CRM 칸반보드', () => {
     test('칸반보드가 로드된다', async ({ page }) => {
       await page.goto('/crm')
       // 탭이 표시되는지 확인
       await expect(page.getByText('문의')).toBeVisible()
       await expect(page.getByText('영업')).toBeVisible()
       await expect(page.getByText('시공')).toBeVisible()
     })

     test('탭 전환이 동작한다', async ({ page }) => {
       await page.goto('/crm')
       await page.getByText('영업').click()
       // 영업 탭의 파이프라인 컬럼이 보이는지
       await expect(page.getByText('먼저연락X')).toBeVisible()
     })

     test('검색이 동작한다', async ({ page }) => {
       await page.goto('/crm')
       const searchInput = page.getByPlaceholder('주소, 고객명, 전화번호 검색')
       await expect(searchInput).toBeVisible()
       // 검색 입력 후 카드 필터링 확인 (데이터 의존적이므로 검색 input 존재만 확인)
       await searchInput.fill('테스트')
       // 입력이 반영되는지 확인
       await expect(searchInput).toHaveValue('테스트')
     })

     test('카드 클릭 시 상세 모달이 열린다', async ({ page }) => {
       await page.goto('/crm')
       // 첫 번째 카드 클릭 (카드가 있는 경우)
       const firstCard = page.locator('[data-testid="kanban-card"]').first()
       if (await firstCard.isVisible()) {
         await firstCard.click()
         // 모달이 열리는지 확인
         await expect(page.locator('[data-testid="detail-modal"]')).toBeVisible()
       }
     })
   })
   ```

4. KanbanCard.tsx에 data-testid="kanban-card" 추가.
   DetailModal.tsx에 data-testid="detail-modal" 추가.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Playwright 테스트 파일 존재, TEST_MODE 미들웨어 우회 동작, npm run build 통과</done>
</task>

</tasks>

<verification>
npm run build 통과.
e2e/crm.spec.ts 존재.
middleware.ts에 TEST_MODE 우회 로직 존재.
칸반 검색바 + 담당자 필터 UI 존재.
상세 모달에 견적서/제안서 연결 버튼 존재.
</verification>

<success_criteria>
- 상세 모달에서 "견적서 작성" 클릭 시 /estimate/new?crmId=...로 이동
- 상세 모달에서 "제안서 작성" 클릭 시 /proposal?address=...로 이동
- 칸반 검색바로 주소/고객명/전화번호 실시간 필터링
- 담당자 필터 드롭다운 동작
- Playwright 테스트 파일 생성 + 빌드 통과
- middleware.ts에 TEST_MODE 인증 우회 추가
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm/09-28-SUMMARY.md`
</output>

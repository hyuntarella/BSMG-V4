---
phase: 09-crm
plan: 25
type: execute
wave: 3
depends_on: [24]
files_modified:
  - components/crm/DetailModal.tsx
  - components/crm/DetailField.tsx
  - components/crm/CommentSection.tsx
  - components/crm/ProgressTimeline.tsx
  - components/crm/ActionButtons.tsx
  - components/crm/KanbanBoard.tsx
autonomous: true
requirements: [CRM-25]
must_haves:
  truths:
    - "카드 클릭 시 상세 모달이 열린다"
    - "모달에서 전체 필드를 인라인 편집할 수 있다"
    - "댓글 목록이 표시되고 새 댓글을 작성할 수 있다"
    - "진행 타임라인에 날짜별 단계가 표시된다"
    - "지도/거리뷰/내비/문자/전화 액션 버튼이 동작한다"
  artifacts:
    - path: "components/crm/DetailModal.tsx"
      provides: "CRM 상세 모달 메인"
    - path: "components/crm/CommentSection.tsx"
      provides: "댓글 목록 + 입력"
    - path: "components/crm/ProgressTimeline.tsx"
      provides: "진행 단계 타임라인"
    - path: "components/crm/ActionButtons.tsx"
      provides: "외부 링크 액션 버튼"
  key_links:
    - from: "components/crm/DetailModal.tsx"
      to: "/api/crm/[id]"
      via: "PATCH fetch for inline edits"
    - from: "components/crm/CommentSection.tsx"
      to: "/api/crm/[id]/comments"
      via: "GET + POST fetch"
---

<objective>
CRM 상세 모달 — 카드 클릭 시 전체 필드 편집, 댓글, 진행 타임라인, 액션 버튼을 포함한 상세 뷰.

Purpose: GAS CRM의 상세 보기를 모달로 재구현. 인라인 편집 + 댓글 + 외부 링크 통합.
Output: 상세 모달 + 서브 컴포넌트 5개
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-crm/09-23-SUMMARY.md
@.planning/phases/09-crm/09-24-SUMMARY.md

Interfaces:
```typescript
// lib/notion/types.ts
export interface CrmRecord { /* 26 fields */ }
export interface CrmComment { id: string; content: string; createdTime: string; createdBy: string; }
```

API endpoints (from Plan 23):
- PATCH /api/crm/[id] — body: Partial<CrmRecord>
- GET /api/crm/[id]/comments — returns CrmComment[]
- POST /api/crm/[id]/comments — body: { content: string }
</context>

<tasks>

<task type="auto">
  <name>Task 1: DetailModal + DetailField 인라인 편집</name>
  <files>components/crm/DetailModal.tsx, components/crm/DetailField.tsx</files>
  <action>
1. components/crm/DetailField.tsx ('use client'):
   - Props: label: string, value: string | null, type: 'text' | 'select' | 'multiselect' | 'number' | 'date' | 'phone' | 'email' | 'url', options?: string[], onSave: (value) => void
   - 표시 모드: label + value. 클릭 시 편집 모드 전환.
   - 편집 모드: type에 따라 input/select/multi-checkbox 렌더링. Enter 또는 blur 시 onSave 호출. Escape 시 취소.
   - select: options 드롭다운. multiselect: 체크박스 목록.
   - phone 타입: tel: 링크 아이콘 추가.
   - 빈 값: "미입력" 텍스트 (text-gray-300 italic).

2. components/crm/DetailModal.tsx ('use client'):
   - Props: record: CrmRecord | null, isOpen: boolean, onClose: () => void, onUpdate: (id, partial) => void
   - 모달 오버레이: fixed inset-0 z-50, bg-black/50, 클릭 시 닫기.
   - 모달 본체: bg-white rounded-xl max-w-lg w-full max-h-[90vh] overflow-y-auto mx-4.
   - 헤더: 주소(제목) + 닫기(X) 버튼.
   - 필드 그룹 (섹션별 구분):
     - 기본정보: 주소(title), 고객명, 전화번호, 고객이메일, 문의채널
     - 영업정보: 담당자(select, 이창엽/박민우/미배정), 단계(select), 파이프라인(select — 현재 단계의 파이프라인만 options), 계약상태(select)
     - 시공정보: 시공분야(multiselect), 시공평수(text)
     - 금액: 견적금액(number), 계약금액(number), 착수금(number), 잔금(number)
     - 일정: 문의일자(date), 견적방문일자(date), 견적서발송일(date), 견적서열람일(date), 잔금완료(date)
     - 링크: 구글드라이브URL(url), 견적서웹URL(url)
   - 각 필드를 DetailField로 렌더링. onSave 시 PATCH /api/crm/[record.id] 호출 + onUpdate 콜백.
   - 파이프라인 select의 options는 현재 record.stage에 따라 STAGE_MAP[stage]를 사용.
   - 하단에 ActionButtons, CommentSection, ProgressTimeline 배치.
  </action>
  <verify>
    <automated>npx tsc --noEmit components/crm/DetailModal.tsx components/crm/DetailField.tsx</automated>
  </verify>
  <done>상세 모달이 열리고 전체 필드 인라인 편집이 가능하며 편집 시 API 호출</done>
</task>

<task type="auto">
  <name>Task 2: 댓글 + 타임라인 + 액션 버튼 + 칸반 연결</name>
  <files>components/crm/CommentSection.tsx, components/crm/ProgressTimeline.tsx, components/crm/ActionButtons.tsx, components/crm/KanbanBoard.tsx</files>
  <action>
1. components/crm/CommentSection.tsx ('use client'):
   - Props: pageId: string
   - useEffect로 GET /api/crm/{pageId}/comments 조회, comments 상태 저장.
   - 댓글 목록: 작성자 + 내용 + 시간 (time ago 포맷). 최신순 정렬.
   - 새 댓글 입력: textarea + 전송 버튼. POST /api/crm/{pageId}/comments. 전송 후 목록에 추가.
   - 로딩 상태, 빈 상태 처리.

2. components/crm/ProgressTimeline.tsx:
   - Props: record: CrmRecord
   - 세로 타임라인: 5단계 — 문의일자, 견적방문일자, 견적서발송일, 견적서열람일, 잔금완료.
   - 각 단계: 날짜가 있으면 파란 원 + 날짜 표시, 없으면 회색 원 + "미완료".
   - 단계 간 세로 선으로 연결. 완료된 단계는 파란 선, 미완료는 점선.

3. components/crm/ActionButtons.tsx:
   - Props: record: CrmRecord
   - 가로 버튼 바 (flex gap-2):
     - 지도: 네이버 지도 (https://map.naver.com/v5/search/{encodeURIComponent(record.address)}). target="_blank".
     - 거리뷰: 카카오 로드뷰 (https://map.kakao.com/?q={encodeURIComponent(record.address)}). target="_blank".
     - 내비: Tmap (tmap://route?goalname={address}). 모바일에서만 동작, 데스크탑에서는 안내 텍스트.
     - 문자: sms:{record.phone}
     - 전화: tel:{record.phone}
   - 전화번호 없으면 문자/전화 버튼 disabled.
   - 각 버튼: 아이콘 + 텍스트, 작은 사이즈.

4. KanbanBoard.tsx 수정:
   - KanbanCard onClick 시 selectedRecord 상태 설정.
   - DetailModal 렌더링: isOpen={!!selectedRecord}, record={selectedRecord}, onClose, onUpdate(records 로컬 업데이트).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>카드 클릭 시 상세 모달이 열리고, 댓글/타임라인/액션 버튼이 모두 동작하며, 인라인 편집으로 Notion 데이터 업데이트 가능</done>
</task>

</tasks>

<verification>
npm run build 통과.
카드 클릭 → 모달 → 필드 편집 → API 호출 → 로컬 업데이트 흐름.
댓글 조회/작성, 타임라인 표시, 외부 링크 버튼 동작.
</verification>

<success_criteria>
- 상세 모달에서 26개 필드 표시 + 인라인 편집 가능
- 댓글 조회/작성 동작
- 진행 타임라인에 날짜별 단계 표시
- 지도/거리뷰/내비/문자/전화 버튼이 올바른 URL로 연결
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm/09-25-SUMMARY.md`
</output>

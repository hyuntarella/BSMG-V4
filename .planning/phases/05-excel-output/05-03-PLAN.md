---
phase: 05-excel-output
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/excel/generateWorkbook.ts
  - lib/utils/numberToKorean.ts
autonomous: true
requirements:
  - EXCEL-03
must_haves:
  truths:
    - "Sheet1(표지)에 관리번호, 일자, 고객명+'귀하', 현장명이 정확히 채워진다"
    - "한글 금액이 '일금 팔천이백오십만원 정' 형식으로 정확히 변환된다"
    - "E11에 한글 금액, K14에 Sheet2 총액, D19에 보증조건 텍스트가 채워진다"
  artifacts:
    - path: "lib/utils/numberToKorean.ts"
      provides: "numberToKorean 함수 (일금 X원 정 형식)"
      exports: ["n2k", "toKoreanAmount"]
    - path: "lib/excel/generateWorkbook.ts"
      provides: "Sheet1(표지) 데이터 채우기"
  key_links:
    - from: "lib/excel/generateWorkbook.ts"
      to: "lib/utils/numberToKorean.ts"
      via: "toKoreanAmount import"
      pattern: "import.*toKoreanAmount.*numberToKorean"
---

<objective>
표지(Sheet1) 생성 -- 템플릿의 Sheet1(표지)에 관리번호, 일자, 고객명, 현장명, 한글 금액, 총액, 보증조건을 채운다. numberToKorean.ts에 "일금 X원 정" 형식의 변환 함수를 추가한다.

Purpose: 견적서 표지는 고객에게 전달되는 첫 페이지. 관리번호, 금액, 보증조건이 정확해야 신뢰도를 확보한다.

Output: Sheet1 표지 완성 + toKoreanAmount 유틸 함수.
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-excel-output/05-01-SUMMARY.md
@lib/excel/generateWorkbook.ts
@lib/utils/numberToKorean.ts
@lib/estimate/types.ts

<interfaces>
From lib/utils/numberToKorean.ts (현재):
```typescript
export function n2k(n: number): string;
// n2k(3900000) → "삼백구십만"
```

From lib/estimate/types.ts:
```typescript
export interface Estimate {
  mgmt_no?: string; date: string;
  customer_name?: string; site_name?: string;
  sheets: EstimateSheet[];
}
export interface EstimateSheet {
  type: Method; warranty_years: number; warranty_bond: number;
  grand_total: number; items: EstimateItem[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: toKoreanAmount 함수 추가</name>
  <files>lib/utils/numberToKorean.ts</files>
  <action>
기존 n2k 함수는 유지하고, 견적서 표지용 toKoreanAmount 함수를 추가한다.

```typescript
/**
 * 견적서 표지용 한글 금액
 * toKoreanAmount(8250000) → "일금 팔백이십오만원 정(₩8,250,000)"
 * toKoreanAmount(3900000) → "일금 삼백구십만원 정(₩3,900,000)"
 */
export function toKoreanAmount(n: number): string {
  const korean = n2k(n);
  const formatted = Math.round(n).toLocaleString('ko-KR');
  return `일금 ${korean}원 정(₩${formatted})`;
}
```

주의: n2k(0)은 "영"을 반환하므로 toKoreanAmount(0)은 "일금 영원 정(₩0)"이 된다. 이것은 정상.
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npx tsx -e "const { toKoreanAmount } = require('./lib/utils/numberToKorean'); console.log(toKoreanAmount(8250000)); console.log(toKoreanAmount(3900000)); console.log(toKoreanAmount(0));"</automated>
  </verify>
  <done>
    - toKoreanAmount(8250000) === "일금 팔백이십오만원 정(₩8,250,000)"
    - toKoreanAmount(3900000) === "일금 삼백구십만원 정(₩3,900,000)"
    - 기존 n2k 함수 동작 유지
  </done>
</task>

<task type="auto">
  <name>Task 2: Sheet1(표지) 데이터 채우기</name>
  <files>lib/excel/generateWorkbook.ts</files>
  <action>
generateFromTemplate (또는 plan-01에서 만든 함수) 내에서 Sheet1(표지)에 데이터를 채운다.

1. **Sheet1 접근**: `wb.getWorksheet(1)` 또는 이름으로 접근.

2. **셀 매핑** (템플릿 구조에 따라 조정 필요 -- 먼저 템플릿을 로드해서 구조를 콘솔에 출력하여 확인):
   - D6: 관리번호 (`estimate.mgmt_no ?? ''`)
   - D7: 일자 (`estimate.date`)
   - D8: 고객명 + "귀하" (`(estimate.customer_name ?? '') + ' 귀하'`)
   - D9: 현장명 (`estimate.site_name ?? ''`)
   - E11: 한글 금액 (`toKoreanAmount(calcResult.grandTotal)`)
   - K14: 총액 숫자 (Sheet2의 합계(10만절사) 값, 숫자 타입으로 넣고 numFmt는 템플릿 서식 유지)
   - D19: 보증조건 텍스트 (`하자보증기간: ${sheet.warranty_years}년, 하자이행보증증권: ${sheet.warranty_bond}억원`)

3. **주의사항**:
   - 셀 위치(D6, D7 등)는 템플릿 파일의 실제 구조를 확인한 후 조정한다. 실행 시 먼저 Sheet1의 모든 셀 값을 출력하여 라벨 위치를 확인하고, 라벨 옆 셀에 값을 넣는다.
   - 값만 설정하고 기존 서식(폰트, 테두리, 정렬)은 건드리지 않는다.
   - `import { toKoreanAmount } from '@/lib/utils/numberToKorean'` 추가.
   - calc()을 호출하여 grandTotal을 구한다.

4. 복합+우레탄 둘 다 있을 때: 표지의 금액은 첫 번째 시트(보통 복합)의 grandTotal을 표시한다.
  </action>
  <verify>
    <automated>cd "C:/Users/나/BSMG-V4" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Sheet1 표지에 관리번호, 일자, 고객명+"귀하", 현장명이 채워진다
    - E11에 "일금 X원 정(₩X)" 형식의 한글 금액이 들어간다
    - K14에 총액 숫자가 들어간다
    - D19에 보증조건 텍스트가 들어간다
    - npm run build 통과
  </done>
</task>

</tasks>

<verification>
npm run build 성공. toKoreanAmount 함수가 정확한 한글 금액 문자열을 반환.
</verification>

<success_criteria>
- 표지의 모든 필드가 채워진다
- 한글 금액 변환이 정확하다
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/05-excel-output/05-03-SUMMARY.md`
</output>

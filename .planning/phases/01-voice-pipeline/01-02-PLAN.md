---
phase: 01-voice-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/estimate/EstimateEditor.tsx
autonomous: false
requirements:
  - VUX-01
must_haves:
  truths:
    - "음성 가이드 완료 후 TTS가 복합/우레탄 총액을 한국어로 읽어준다"
    - "각 필드 입력 후 TTS가 반영 내용을 확인해준다"
    - "전체 파이프라인이 음성 녹음→STT→파싱→시트 생성→TTS 총액 안내까지 끊김 없이 동작한다"
  artifacts:
    - path: "components/estimate/EstimateEditor.tsx"
      provides: "onComplete 콜백에서 총액 TTS 피드백 생성"
  key_links:
    - from: "hooks/useVoiceFlow.ts onComplete"
      to: "components/estimate/EstimateEditor.tsx initFromVoiceFlow"
      via: "callback → initFromVoiceFlow → setEstimate → TTS"
      pattern: "initFromVoiceFlow.*playTts"
    - from: "hooks/useEstimate.ts initFromVoiceFlow"
      to: "lib/estimate/buildItems.ts"
      via: "buildItems call inside initFromVoiceFlow"
      pattern: "buildItems"
---

<objective>
음성 가이드 플로우 완료 후 TTS가 생성된 견적서의 총액을 한국어로 읽어주고, 전체 end-to-end 파이프라인을 사용자가 직접 검증한다.

Purpose: VUX-01 요구사항 — "모든 음성 입력 후 TTS가 결과와 총액 변화를 한국어로 읽어준다". 현재 onComplete에서 "견적서를 생성합니다"만 말하고 실제 총액을 읽지 않는다.
Output: 총액 포함 TTS 피드백 + 사용자 E2E 검증 완료
</objective>

<execution_context>
@C:/Users/나/BSMG-V4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/나/BSMG-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-voice-pipeline/01-CONTEXT.md
@.planning/phases/01-voice-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From useEstimate — initFromVoiceFlow sets estimate state synchronously -->
<!-- After initFromVoiceFlow, estimate.sheets will have items with grand_total -->

From hooks/useEstimate.ts initFromVoiceFlow:
```typescript
// After calling initFromVoiceFlow, the estimate state is updated with:
// - estimate.m2 = data.area
// - estimate.wall_m2 = data.wallM2
// - estimate.sheets[0] = { type: '복합', grand_total: number, items: [...] }
// - estimate.sheets[1] = { type: '우레탄', grand_total: number, items: [...] }
```

From lib/estimate/buildItems.ts:
```typescript
export function buildItems(input: BuildItemsInput): {
  items: EstimateItem[]
  calcResult: { subtotal: number; overhead: number; profit: number; grandTotal: number }
}
```

From CLAUDE.md TTS feedback patterns:
```
| 상황 | TTS 응답 |
| 데이터 입력 | "복합방수 백오십제곱미터, 사다리차 이틀 반영. 총액 오백팔십만원." |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 음성 가이드 완료 후 총액 TTS 피드백 추가</name>
  <files>components/estimate/EstimateEditor.tsx</files>
  <read_first>
    - components/estimate/EstimateEditor.tsx (onComplete 콜백 — L199-212, voice.playTts 사용 패턴)
    - hooks/useEstimate.ts (initFromVoiceFlow가 반환하는 시점과 estimate state 갱신 타이밍)
    - hooks/useVoiceFlow.ts (onComplete 호출 위치 — goToNextEmpty L153-159)
    - lib/voice/voiceFlow.ts (getApplyFeedback 패턴 참고)
  </read_first>
  <action>
**문제:** `EstimateEditor.tsx` L199-212의 onComplete 콜백이 `initFromVoiceFlow`를 호출하고 탭을 전환하지만, TTS로 총액을 읽어주지 않는다. `useVoiceFlow.ts` L157에서 "견적서를 생성합니다"만 말한다.

**해결 방식:** onComplete 콜백에서 initFromVoiceFlow 호출 후, buildItems를 직접 호출하여 grand_total을 계산하고 TTS로 읽어준다. (initFromVoiceFlow는 setState를 통해 비동기 갱신하므로, 콜백 내에서 즉시 estimate.sheets를 읽으면 이전 값이 나온다.)

구체적 변경 (`components/estimate/EstimateEditor.tsx`):

1. 상단 import에 추가:
```typescript
import { buildItems } from '@/lib/estimate/buildItems'
import { getAR } from '@/lib/estimate/areaRange'
```

2. onComplete 콜백 (L204-212)을 다음으로 교체:
```typescript
onComplete: async (state) => {
  // 시트 생성
  initFromVoiceFlow({
    area: state.area ?? 0,
    wallM2: state.wallM2 ?? 0,
    complexPpp: state.complexPpp,
    urethanePpp: state.urethanePpp,
  })
  setActiveTab('complex-detail')

  // 총액 계산 (initFromVoiceFlow가 setState라 즉시 반영 안 되므로 직접 계산)
  const m2 = state.area ?? 100
  const wallM2 = state.wallM2 ?? 0
  const ar = getAR(m2)

  const complexMethodData = priceMatrix[ar]?.['복합']
  const complexPrices = complexMethodData ? Object.keys(complexMethodData).map(Number).sort((a, b) => a - b) : []
  const complexPpp = state.complexPpp ?? complexPrices[Math.floor(complexPrices.length / 2)] ?? 35000

  const urethaneMethodData = priceMatrix[ar]?.['우레탄']
  const urethanePrices = urethaneMethodData ? Object.keys(urethaneMethodData).map(Number).sort((a, b) => a - b) : []
  const urethanePpp = state.urethanePpp ?? urethanePrices[Math.floor(urethanePrices.length / 2)] ?? 30000

  const complexResult = buildItems({ method: '복합', m2, wallM2, pricePerPyeong: complexPpp, priceMatrix })
  const urethaneResult = buildItems({ method: '우레탄', m2, wallM2, pricePerPyeong: urethanePpp, priceMatrix })

  const complexTotal = complexResult.calcResult.grandTotal
  const urethaneTotal = urethaneResult.calcResult.grandTotal

  // 금액을 만원 단위 한국어로 변환
  const formatWon = (v: number) => {
    const man = Math.round(v / 10000)
    return `${man.toLocaleString()}만원`
  }

  const ttsText = `견적서 생성 완료. 면적 ${m2}제곱미터. 복합 ${formatWon(complexTotal)}, 우레탄 ${formatWon(urethaneTotal)}.`
  addLog('assistant', ttsText)
  await voice.playTts(ttsText)
},
```

3. voice 변수 참조 순서: useVoice (L173-196)가 useVoiceFlow (L199-213)보다 먼저 선언되므로, onComplete에서 `voice.playTts(ttsText)` 호출은 안전하다.

4. `priceMatrix`는 컴포넌트 props이므로 접근 가능. `buildItems`와 `getAR`는 import하면 된다.

**CLAUDE.md 패턴 준수:** TTS 피드백은 "견적서 생성 완료. 면적 150제곱미터. 복합 580만원, 우레탄 420만원." 형태로 CLAUDE.md 4-5의 패턴을 따른다.
  </action>
  <verify>
    <automated>cd C:/Users/나/BSMG-V4 && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/estimate/EstimateEditor.tsx contains `import { buildItems } from '@/lib/estimate/buildItems'`
    - components/estimate/EstimateEditor.tsx contains `import { getAR } from '@/lib/estimate/areaRange'`
    - components/estimate/EstimateEditor.tsx onComplete callback contains `견적서 생성 완료`
    - components/estimate/EstimateEditor.tsx onComplete callback contains `complexResult.calcResult.grandTotal`
    - components/estimate/EstimateEditor.tsx onComplete callback contains `voice.playTts(ttsText)`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>
    - 음성 가이드 완료 시 TTS가 "견적서 생성 완료. 면적 N제곱미터. 복합 N만원, 우레탄 N만원." 형태로 총액을 읽어줌
    - 빌드 에러 없음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 음성 파이프라인 E2E 검증</name>
  <files>none</files>
  <action>
    사용자가 로컬 개발 서버에서 음성 가이드 플로우를 직접 테스트한다. 아래 3가지 시나리오를 검증한다.
  </action>
  <verify>
    <automated>echo "checkpoint: human verification required"</automated>
  </verify>
  <done>사용자가 3가지 테스트 시나리오를 모두 통과시키고 "approved" 입력</done>
  <what-built>
    음성 가이드 플로우 전체 파이프라인:
    1. 음성 가이드 시작 (VoiceBar 탭 또는 "견적" 웨이크워드)
    2. 면적/벽체/복합ppp/우레탄ppp 순차 수집
    3. 파싱 → 시트 생성 → 총액 TTS 피드백
  </what-built>
  <how-to-verify>
    **사전 조건:** 로컬 개발 서버 실행 (`npm run dev`), 마이크 접근 허용

    **테스트 1: 전체 플로우**
    1. `/estimate/new`로 이동 → 새 견적서 생성 후 에디터 페이지로 리다이렉트
    2. 시트가 없는 상태에서 하단 VoiceBar의 마이크 버튼 탭
    3. "150헤베"라고 말하기 → TTS가 "면적 150제곱미터" 확인 → 자동으로 다음 질문 "벽체 면적은요?"
    4. "벽체 30미터"라고 말하기 → TTS 확인 → "복합 평단가는요?"
    5. "3만5천"이라고 말하기 → TTS 확인 → "우레탄 평단가는요?"
    6. "3만"이라고 말하기 → TTS 확인 → "견적서 생성 완료. 면적 150제곱미터. 복합 N만원, 우레탄 N만원."
    7. 복합 시트와 우레탄 시트가 생성되고, 공종 테이블에 항목과 금액이 채워져 있어야 함
    8. 금액이 0이 아닌 실제 값이어야 함 (P매트릭스 데이터 정상 로드 확인)

    **테스트 2: 마디 넘기기**
    1. 새 견적서에서 음성 가이드 시작
    2. "150헤베"라고 면적 입력
    3. "됐어"라고 말하기 → 벽체가 0으로 채워지고 다음 질문으로
    4. 나머지 필드도 "됐어"로 넘기기
    5. 시트가 생성되고 벽체=0, 기본 평단가 적용

    **테스트 3: 취소**
    1. 음성 가이드 시작
    2. "그만"이라고 말하기
    3. TTS "취소했습니다" → 플로우 idle로 리셋 → 시트 미생성

    **확인 사항:**
    - [ ] TTS가 각 단계에서 자연스럽게 응답하는가
    - [ ] 자동 녹음 재개가 TTS 완료 후 ~1.5초 후에 동작하는가
    - [ ] 최종 TTS에 총액이 포함되어 있는가
    - [ ] 생성된 시트의 금액이 0이 아닌가
  </how-to-verify>
  <resume-signal>"approved"를 입력하거나, 발견된 문제를 설명해주세요</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` — 빌드 에러 없음
2. 음성 가이드 E2E 테스트 통과 (human-verify)
3. TTS 피드백에 총액 포함 확인
</verification>

<success_criteria>
- 음성 가이드 완료 시 TTS가 복합/우레탄 총액을 만원 단위로 읽어준다
- 사용자가 E2E 테스트를 통과시켜 전체 파이프라인 동작 확인
- VOICE-01 (파싱→테이블), VOICE-02 (명령 체계), VUX-01 (TTS 피드백) 모두 충족
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-pipeline/01-02-SUMMARY.md`
</output>
